Task 008: 列宽配置持久化

修复内容:
1. DEFAULT_COL_WIDTHS 补充缺失列 (material_category, remark_check_status, remark_check_msg)
2. 修复 <<TreeviewColumnResized>> 事件不工作的问题，改用 <ButtonRelease-1> + 定时轮询
3. _restore_column_widths 优先从 JSON 文件加载（与保存路径一致）
4. _save_column_widths 保存所有列宽并同步到 ConfigManager（统一双存储）
5. _reset_default_widths 同步清除 ConfigManager 配置
6. _on_close 增加 JSON 文件保存（与 _save_column_widths 一致）
7. 新增 _init_column_width_tracking 启动时根据锁定状态设置绑定