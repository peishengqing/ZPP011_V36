# ZPP011 偏差分析器 - Python 依赖清单
# 安装方式：pip install -r requirements.txt

# 数据处理
numpy>=1.21.0
pandas>=1.3.0
openpyxl>=3.0.0

# PPT 生成
python-pptx>=0.6.21

# 可选（GUI截图/图像处理，如需用到再装）
# Pillow>=9.0.0

# Python 内置（无需安装）：sqlite3, tkinter, threading, logging
