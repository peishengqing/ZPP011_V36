# 2026-08-11 工作日志

## 隔离区筛选（用户明确要求：主表筛选 + 隔离区窗口筛选都要做）
- 主表「物料属性」组新增「隔离区」三态下拉（全部/是/否），与「替代料」并列：`filter_panel.py` 加 `quar_combo`，`get_filters()` 写 `_quarantined_is`，`data_frame_model.py.filterAcceptsRow` 加 `_quarantined_is` 分支（是→`_quarantined==1`，否→`==0`）。
- 隔离区对话框「隔离区列表」Tab 新增跨列关键字搜索框（与「失效复核」Tab 一致，防抖 300ms）：`quarantine_dialog.py` 加 `edit_list_search` + `_list_search_timer` + `_on_list_search_text_changed` + `_apply_list_filters`；`_apply_reason_filter` 改为只记录选择并统一走 `_apply_list_filters`，使「隔离原因列头筛选」与「关键字搜索」叠加生效；`_refresh_self` 改为刷新后同时重放两种筛选。

## 顺手修的预存 bug（pyflakes 抓到）
- `quarantine_dialog.py._mark_read_and_remove`：构造 records 时用了 `main_df` 但该变量直到后面才定义 → 右键「设为已读并移出隔离区」必 NameError 崩。已提前定义 `main_df`。
- 清理两个未用 import：`remove_quarantine`、`save_read_status`。
- 三文件 pyflakes + py_compile 全绿，已清 `__pycache__`。

## 主表内部列隐藏（用户问 data_id/fingerprint/_quarantined 三列含义）
- 这三列是内部技术列（关联键/变更哈希/隔离标记），本不该显示。根因：`main_window.py._hidden_column_names` 默认只藏了 `_post_audit_changed`。
- 改：`__init__` 默认隐藏集扩为 `{'_post_audit_changed','data_id','fingerprint','_quarantined'}`；`_apply_column_visibility_by_name` 第2182行也强制加回这4列（防用户在列显隐对话框误勾显示）。

## 偏差率预警看板加关键字搜索（1900+条定位难）
- `deviation_warning_dialog.py` 已有分类筛选（已读/料别/车间/隔离区/替代料），缺跨列关键字搜索。
- 加 `search_edit`（QLineEdit，占位"搜索 物料编码/名称/车间/流程订单/备注…"）+ 防抖 QTimer 300ms（与隔离区窗口一致）。
- 新增 `_on_search_changed`（`_keyword` 小写化）/`_keyword_mask`（跨列 `str.contains` 含关键字即命中，跳过 `_read/data_id/状态/_post_audit_changed`，regex=False 防特殊字符）。
- `_apply_filter` 叠加 `& self._keyword_mask(df)`；导出走 `source_model.getDataFrame()` 自然含关键字筛选。pyflakes+py_compile 全绿，已清 `__pycache__`。

## 发版 v42.76（15:30 推送 / 15:33 出 exe）
- 提交 `2f403d2`（6文件 +164/−33）含本期全部改动：主表隔离区三态筛选、隔离区列表关键字搜索、偏差率看板关键字搜索、流程订单多选、主表隐藏内部列、隔离区右键崩溃修复、失效通知显示物料编码+名称。
- 推送北京时间 2026-08-11 15:30:26，origin/main `99bbee8 → 2f403d2`。
- 打包：`build_pyside6_exe.py --debug`，产物 `dist/ZPP011偏差分析器_v42.76_20260811_153057.exe`（201M），已复制到桌面 `C:\Users\Administrator\Desktop\`。
- 源码备份：`C:\Users\Administrator\.zpp011_audit\source_backups\zpp011_source_v42.76_20260811_153057.zip`。
- 打包 stderr 无害告警：PyInstaller "Duplicate name" zip 噪音 + 旧备份清理被沙箱回收站拦截（SAFE_DELETE_FAIL_CLOSED）+ 4 个不存在的 hidden-import 跳过（drill_down_dialog/settings_dialog/bom_diff/excel_helper，历史遗留，不影响运行）。

## 修复：偏差率看板加隔离区后需重新分析才显示（15:55）
- 现象：偏差率预警看板右键「移入隔离区」→ 直接打开隔离区对话框看不到；但只要做一次「重新分析」就出现了。
- 根因：`deviation_warning_dialog._set_quarantine` 写 SQLite 成功，但内存同步只改了 `main_window.view_model.df._quarantined` 和看板自己的 `original_df`，**漏了推回主表 `source_model`**（`main_window._set_quarantine` 在 3251 行有 `source_model.setDataFrame(df)`）。而隔离区对话框 `_get_master_df()`（2257行）读的是 `source_model` 那份数据，所以改 `view_model.df` 对它无效。重新分析之所以能出现，是因为 `data_service._restore_quarantine_status`（407行）从 SQLite 回填了 `source_model._quarantined`。
- 修法：`_set_quarantine` 写库后仿主表，把带 `_quarantined` 标记的 `main_df` 推回 `source_model.setDataFrame`，并刷新 `_apply_column_visibility_by_name` 与 `stats_cards`。→ 实时更新，无需重新分析。
- pyflakes+py_compile 全绿，已清 `__pycache__`。尚未提交/打包。

## 发版 v42.77（15:59 推送 / 16:02 出 exe）
- 提交 `5e8f511`（2文件 +19/−1）：`deviation_warning_dialog.py`（隔离区实时同步修复）+ `utils/version_history.py`（v42.77 日志）。
- 推送北京时间 2026-08-11 15:59:20，origin/main `2f403d2 → 5e8f511`。
- 打包：`build_pyside6_exe.py --debug`，产物 `dist/ZPP011偏差分析器_v42.77_20260811_155944.exe`（201M），已复制到桌面 `C:\Users\Administrator\Desktop\`。
- 源码备份：`C:\Users\Administrator\.zpp011_audit\source_backups\zpp011_source_v42.77_20260811_155944.zip`。

## Plan A 完成：偏差率看板加隔离区不整表刷新（16:45 落实）
- 用户 approved 的 Plan A 之前被中断未执行，现补完：`deviation_warning_dialog.py._set_quarantine` 把 `source_model.setDataFrame(main_df)` 换成 `source_model.mark_quarantine(ids, flag)`（复用 main_window._set_quarantine 同一套写法）。
- 效果：偏差率看板右键「移入隔离区」后，主表只发单行 dataChanged，**不整表 reset**，故主表滚动位置/列排序/选中行/用户筛选全部保留，加完第1条还能稳稳看到第2条（修复用户反馈的"筛选被清空"回归）。
- `DataFrameModel.mark_quarantine`（data_frame_model.py:64）已存在：就地改 `_data` 的 `_quarantined` 列 + 同步 `_quarantined_rows` 染色缓存 + 单行 dataChanged，不 beginResetModel。

## 偏差率预警看板列表加「单位」列（16:45）
- 根因：`_show_deviation_warning_dialog`（main_window.py:1620）用 `candidates` 白名单挑列，`单位` 不在名单 → 主表虽有 `单位` 列（analyzer 把 组件单位 映射成 单位，sheet5_full.py:75）也被丢弃。
- 改：`candidates` 白名单加 `"单位"`（放在 `物料大类` 之后），看板列表即显示单位列（件/kg/个…），导出与跨列搜索自动带上。
- 两文件 pyflakes+py_compile 全绿（main_window 仅预存未用 import），已清 `__pycache__`。尚未提交/打包。

## 发版 v42.78（16:48 推送 / 16:52 出 exe）
- 提交 `cf3d314`（4文件 +51/−7）：`deviation_warning_dialog.py`（Plan A 就地更新）、`main_window.py`（看板列表加单位列）、`data_frame_model.py`（mark_quarantine 方法）、`utils/version_history.py`（v42.78 日志）。
- 推送北京时间 2026-08-11 16:48:59，origin/main `5e8f511 → cf3d314`。
- 打包：`build_pyside6_exe.py --debug`，产物 `dist/ZPP011偏差分析器_v42.78_20260811_164923.exe`（201M），已复制到桌面 `C:\Users\Administrator\Desktop\`。
- 源码备份：`C:\Users\Administrator\.zpp011_audit\source_backups\zpp011_source_v42.78_20260811_164923.zip`。
- 静态检查：py_compile 全绿；pyflakes 仅 main_window 预存未用 import，无新增 undefined name（F821）。临时 `_tmp_filter_test.py` 已删。
