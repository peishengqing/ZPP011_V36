# 2026-08-12 工作日志

## 偏差率预警看板「料别」筛选加「半成品」按钮
- 用户问预警是否含半成品（答：含，mask 只看 |偏差率|≥10% 且非机械零投料行，不按料别排除）；随后要求加半成品按钮。
- 改动 `gui_pyside6/dialogs/deviation_warning_dialog.py`（5 处）：
  1. UI：「包材」按钮后加「半成品」按钮 → `_set_mat_filter("semi")`。
  2. `_set_mat_filter`：补 `btn_mat_semi.setChecked(mode=="semi")`。
  3. `_mat_mask`：新增 `semi` 分支，`col.str.contains("半成品")` 命中；若数据带 `物料分类` 列且值为半成品也命中。保留 raw/pkg 既有写法。
  4. `_update_button_counts`：料别组补「半成品」条数；空 df 兜底元组也加。
  5. `set_data`：新按钮纳入显隐控制 + 复位未勾选；`_filter_desc` 映射加 `"semi":"半成品"`（影响导出文件名）。
- 半成品判定：analyzer `infer_material_type` 前缀 40/41 → 物料类型='半成品'（`analysis/analyzer.py:65/79`）；`物料分类` 经 np.select 描述含"半成品"→'半成品'（:378-387）。注：预警对话框传入的 warnings_df 不含 `物料分类` 列（main_window candidates 白名单未列），故实际只靠 物料类型=='半成品' 命中。
- 静态检查：pyflakes 0 / py_compile 0，`__pycache__` 已清。尚未提交/打包（待用户决定）。

## 自动隔离区规则条件丰富化（方案A：现有结构加新条件）
- 用户要求"自动隔离区规则的条件也丰富一下"，对比已读规则（auto_read_rules.py 用 CONDITION_TYPES 注册表约20种）后，选定方案A：在隔离区规则固定字段结构里新增一组条件，不动 JSON 格式、旧4条件继续生效、低风险。
- 改动 `core/auto_quarantine.py`：
  1. `DEFAULT_RULE`/`_RULE_FIELDS` 新增 9 个字段（dev_rate_required/min/max、mat_code_prefix、workshop_required/value、remark_mode、name_exclude_keywords、dev_qty_required/min/max），默认全关/空，向后兼容旧配置。
  2. `_normalize_rule`：新数字字段兜底 float；remark_mode 仅接受 off/has/none。
  3. `compute_auto_quarantine_ids`：新增列探测（偏差率/物料编码/车间/备注/偏差数量）并传入 `_match_single_rule`。
  4. `_match_single_rule`：新增 6 个 AND 分支——偏差率范围(开区间)、物料编码前缀(逗号多值OR)、车间、是否备注(has/none)、偏差数量范围(开区间)、名称不含(逗号多值排除)。返回掩码扩充为 10 路 AND。
  5. `build_rule_summary`：新条件中文预览。
- 改动 `gui_pyside6/dialogs/auto_quarantine_rule_dialog.py`：
  - 编辑区包进 QScrollArea（条件增多防截断）；最小高度 560→680。
  - 新增控件：偏差率范围(勾选+双spin)、物料编码前缀(文本框)、车间(勾选+文本框)、备注要求(三态combo 全部/有/无)、名称不含(文本框)、偏差数量范围(勾选+双spin)。
  - `_commit_editor`/`_load_rule_to_editor`/`_collect_preview` 同步读写 9 新字段 + 信号连接（valueChanged/currentIndexChanged）。
- 无头测试 9 例全过（含默认规则向后一致、多规则 OR）。注意：`_normalize_rule` 会把未指定字段用 DEFAULT_RULE 补齐（旧4条件默认开），故新增规则仍克隆"包材箱类负损"基底——安全（空规则会因 AND 全 True 误伤全部，故保留默认开）。要用纯新条件需手动取消旧4勾选。
- 静态检查 pyflakes 0 / py_compile 0。尚未提交（连同此前未提交的 deviation_warning_dialog.py 半成品/单位、auto_read_rules.py 前缀多值 一并待打包）。

## ZPP011 优化方案(第三版) 审阅 + 12K 行时间基线实测
- 用户贴 `Desktop/zpp011_fix_optimization_plan.md`（标注第三版，基于 v42.78/cf3d314，15 条 P0/P1/P2）。
- 逐条核对真实代码：P1-6(3888行)/P1-9(类函数内动态定义)/P1-10(~7处裸except pass)/P2-11(apply行号) 均属实；P0-4(`df.at` 在 net_offset.py 164/174/203-206) 真实存在（我首轮误判"不存在"是 grep 被 head 截断，已纠正）；P0-5 实际 9-10 处 to_dict 非文档说的 7。
- **三处修复不能照抄**：①P0-1 的 `df.index+2` 是潜在回归——当前 openpyxl 重读是防空行偏移的纠错保护，删掉只剩 fallback；②P0-2 的 merge 方案漏了「备注原因」变动检测（该函数的核心职责）；③P2 偏差率格式化 `np.where` 片段对 NaN 处理有误，当前 `.apply` 写法正确。所有"节省 N 秒"均为估算未实测。
- **基线实测**（Python311，真实样例 6892 行平铺成 12K 行，脚本在 Temp/zpp_bench.py）：
  - P0-1 Excel 双重读取 **4.92s**（read_excel 2.67 + openpyxl 重读 2.25）——文档估 3-8s **准**，为最大头，但 2.25s 是纠错保护。
  - P0-2 iterrows 双循环 **0.51s**——文档估 5-15s **夸大 10-30 倍**，几乎不值得动。
  - P0-4 `df.at` 循环 **0.375s → 向量化 0.008s（49x）**——文档估 2-5s 偏高，但向量化是低风险真收益。
  - P0-5 to_dict **0.45s**——文档"50-100MB 峰值"未验证，实耗可忽略。
  - P0 四项累计实测 **6.26s**（文档估 12-33s，偏高 2-5 倍）。
- **结论/建议**：只动 P0-4（向量化，安全 49x）；P0-1 保留（2.25s 纠错保护，除非确认源文件从不跳空行才考虑去）；P0-2/P0-3/P0-5 不值得（亚秒级，重构风险 > 收益）。用户选 B（先测基线再决定），基线已出，待用户确认是否落地 P0-4。
- 注：v42.79 本地已提交 `47d93f3`（6 源码文件），但 `git push` 此前被用户打断未推。

## P0-4 已落地（net_offset.py df.at → loc 向量化）
- 用户确认"可以先落地"。改动 `analysis/net_offset.py` 原 198-206 行：4 层嵌套 `for idx in code_to_indices[code]: df.at[idx,...]` 逐行 4 次写，改为按 `idxs=code_to_indices[code]` 一次性 `df.loc[idxs, '净偏差数量'/'净偏差金额']` 批量写；`_替代料组` 字符串拼接改 `'组_' + df.loc[idxs, order_col].astype(str) + '_' + root[:8]`（逐行读 order_col 合并为单次列读取）。
- 验证：py_compile OK + pyflakes 0；与 v42.79 HEAD 版（git show HEAD）单元级数值等价性 PASS（净偏差数量/金额/_替代料组 三列逐行一致）；12K 行触发写循环 0.646s→0.259s（2.5× 加速）。
- 工作区 M `analysis/net_offset.py`，尚未提交。v42.79 仍停留本地 `47d93f3` 未 push；P0-4 待纳入发行。

## 隔离区搜索框"搜不到"排查结论（非 bug）
- 用户报"搜索框不能筛选隔离原因（如 自动规则[第3条]）"。实测 v42.78/cf3d314 与当前 `_apply_list_filters` 均遍历所有列（含隔离原因），逻辑正确；无头测试搜「第3」能命中「自动规则[第3条]」。
- 真因：用户数据里隔离原因写的是「第三条」（中文数字）而非「第3」（阿拉伯数字），子串匹配自然搜不到。用户确认输「第三条」即可搜到 → 非代码问题，关闭。
- 顺带核：quarantine_dialog.py 的 `FilterHeader.mousePressEvent` 内弹模态菜单会被 mouseRelease 关闭（▼ 三角菜单一闪而过）确为真实 bug，但用户说"隔离区不用管"，故本次 revert 该修复回到 v42.79，未交付。

## 彩罐/托盘/手包袋 负损(含未投料) 独立看板 → v42.80 已交付
- 用户要把"彩罐/托盘/手包袋 负损(含未投料)"从自动隔离区规则里拆出来，单独做**独立看板**（不与隔离区放一起）。
- 新建 `gui_pyside6/dialogs/neg_loss_dashboard_dialog.py`（仿 deviation_warning_dialog 模板）：读主表全量 → 按「物料名称含关键词(默认 彩罐,托盘,手包袋，可编辑) + 负损(实际<定额，含未投料勾选则 0≤实际<定额)」筛选 → 表格优先展示「备注原因」列 → 右键/按钮可手动加入隔离区。
- `main_window.py`：import + 工具栏 🟠 负损看板 按钮（action_btn_neg_loss → _show_neg_loss_dashboard，传主表 df 子集）。
- 无头逻辑测试 PASS（含未投料 2 条 / 不含 1 条 / 关键词切换正确）；py_compile OK + pyflakes 0（main_window 告警均为预存未用导入）。
- 交付：commit `f498331`(v42.80) 含 看板3文件 + P0-4(net_offset.py) + version日志；push 北京时间 2026-08-12 16:36:35（47d93f3→f498331）；exe `dist/ZPP011偏差分析器_v42.80_20260812_163705.exe` 已复制到桌面。
- 不提交 runtime 配置：config/auto_quarantine_config.json、config/column_widths.json（用户专属，git 已跟踪但本次 M 不纳入）。
