# 2026-08-15

## 🔍 代码质量静态扫描 (ZPP011_v2)

- **背景：** 裴哥要求"检查代码质量"。本地 python 环境（envs/default, 3.13.12）未预装静态分析器。
- **工具：** 已 `pip install ruff==0.16.3`，可执行文件路径：
  `C:\Users\Administrator\.workbuddy\binaries\python\envs\default\Scripts\ruff.exe`
  （注意：`python -m ruff` 解析到另一个 3.13.12 解释器找不到模块，必须用上面 exe 本体调用）
- **扫描命令：** `ruff check . --exclude "build,dist_run,dist,backups,htmlcov" --select <类别>`
- **总量：** 1272 处 ruff 告警，其中 646 处可 `--fix` 自动修。

### 关键结论：真 bug 极少，主要是风格/复杂度债务
- **F811 backup_manager.py:303** `get_audit_db_path` 重复定义 → 两处都返回 `~/.zpp011_audit/audit_log.db`，等价，纯冗余（非行为 bug）。
- **PLR0124 read_status.py:422** `f != f` → 误报，实为故意的 NaN 检测守卫，正确，勿改。
- **F541 (12处)** 全部为纯静态文本的废 f-prefix，无漏写 `{}` 的真 bug，可安全 `--fix`。
- **B023 data_service.py:525** lambda 用循环变量 `key` → `.map` 同步执行，安全。
- **B008 (8处)** 默认参数调 `RGBColor(...)` → 返回不可变对象，安全。
- **RUF012 (7处)** 可变类属性默认值 → 仅当实例原地修改该类属性才会有共享状态问题，需逐处确认，未证实为 bug。

### 真正值得处理的（按严重度）
1. **复杂度 C901**：84 个函数超阈值。最夸张：`gui_pyside6/models/data_frame_model.py:496 filterAcceptsRow`=119、`analysis/analyzer.py:85 do_analysis_v2`=90、`modules/audit/filters/filter_engine.py:20 apply`=63、`gui_pyside6/main_window.py:694 _show_audit_changes_dialog`=60。维护风险最高。
2. **BLE001 裸 except (251处)** + **S110 try-except-pass (66处)**：过度吞异常，掩盖真实错误，稳健性最大隐患。
3. **DTZ005 datetime.now 无时区 (50处)**：时区正确性，报表时间戳可能出错。
4. **风格/现代化可自动修**：F401(151)/I001(165)/UP009(131)/UP006(93)/F841(34)/UP031(50)。

### 未覆盖
- 本环境无 pytest / PySide6，`pytest` 无法跑。测试是否通过需在工程真实环境验证。

### 待裴哥决策
- 是否自动修可 fix 的 646 处（低风险清理）？
- 是否优先重构 top 复杂度函数（filterAcceptsRow / do_analysis_v2）？
- 是否收紧 BLE001/S110 的异常捕获？
